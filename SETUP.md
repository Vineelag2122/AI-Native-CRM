# Frontend Setup
cd frontend
npm install

# Backend Setup
cd ../backend
npm install

# Channel Service Setup
cd ../channel-service
npm install

# Then run each in separate terminals:
# Terminal 1 - Frontend
cd frontend
npm start

# Terminal 2 - Backend
cd backend
npm run dev

# Terminal 3 - Channel Service
cd channel-service
npm run dev
